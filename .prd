[
  {
    "id": "gh-pages-deploy",
    "subject": "Init git repo, push to AnEntrypoint/flatload, enable GH Pages from docs/ folder",
    "status": "pending",
    "description": "1) git init in C:/dev/pl, 2) proper .gitignore, 3) run bun build to produce docs/, 4) git add & commit, 5) gh repo create AnEntrypoint/flatload or push to existing, 6) enable GH Pages via gh api to serve from docs/ on main branch, 7) verify deployed URL returns HTTP 200.",
    "effort": "medium",
    "category": "infra",
    "blocking": [],
    "blockedBy": [],
    "acceptance": [
      "git remote origin points to github.com/AnEntrypoint/flatload",
      "GH Pages URL returns HTTP 200 with HTML containing Flatload",
      "docs/ committed and pushed to main"
    ],
    "edge_cases": [
      "gh CLI must be authenticated",
      "repo may already exist at AnEntrypoint/flatload"
    ]
  }
]